# API Package

The Flutter source app has no remote API integrations, so there was no REST layer to migrate.
This package is reserved for future Retrofit services if the app grows beyond local-only data.
