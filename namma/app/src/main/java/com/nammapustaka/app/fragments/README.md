# Fragments Package

The native migration uses a Compose-first single-activity architecture with Navigation Compose.
This package is reserved in case the project later introduces Fragment-hosted flows.
